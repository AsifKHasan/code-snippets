## OpenClipart Scraper

Async CLI tool to scrape OpenClipart metadata and download assets.

### Install
pip install -r requirements.txt

### Run
./run.sh
./run.sh --json
./run.sh --url <url>

Windows:
run.bat --json
